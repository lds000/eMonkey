<Window x:Class="eMonkey.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:eMonkey"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ListView x:Name="PatientVisitList" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Select">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                                  DataContext="{Binding}"
                                  Click="ToggleCheckbox"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Time" DisplayMemberBinding="{Binding AppointmentTime}" Width="80"/>
                    <GridViewColumn Header="Patient Name" DisplayMemberBinding="{Binding PatientName}" Width="150"/>
                    <GridViewColumn Header="Insurance" DisplayMemberBinding="{Binding Insurance}" Width="150"/>
                    <GridViewColumn Header="Provider" DisplayMemberBinding="{Binding Provider}" Width="150"/>
                    <GridViewColumn Header="Reason" DisplayMemberBinding="{Binding VisitReason}" Width="200"/>
                    <GridViewColumn Header="Gender" DisplayMemberBinding="{Binding Gender}" Width="50"/>
                    <GridViewColumn Header="Age" DisplayMemberBinding="{Binding Age}" Width="50"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding VisitStatus}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
